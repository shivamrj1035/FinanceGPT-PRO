# Core Dependencies - Optimized for Hackathon Speed

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
websockets==12.0

# Gemini AI - The star of our show
google-genai==0.1.0

# Database - SQLite for zero setup
sqlalchemy==2.0.25
aiosqlite==0.19.0  # Async SQLite support

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
pydantic==2.5.3
pydantic-settings==2.1.0

# Data Processing
pandas==2.1.4
numpy==1.26.3

# Basic ML (only if time permits)
scikit-learn==1.4.0

# Utilities
python-dotenv==1.0.0
aiofiles==23.2.1
httpx==0.26.0
faker==22.0.0  # For mock data generation

# CORS support
python-cors==1.0.0

# Optional (skip if short on time)
# redis==5.0.1  # Only if we need caching
# prophet==1.1.5  # Only if we do forecasting
# xgboost==2.0.3  # Only if we need advanced ML